// 新しいコメントを追加したいdivのidが'comments'だと仮定します
document.getElementById('comments').insertAdjacentHTML('beforeend', '<%= j render @comment %>');

// コメントフォームをクリア
document.getElementById('new_comment').reset();
